#!/usr/bin/env ruby
# git get-pr-id - display pull request (previously set with git-set-pr)

require 'yaml'

settings_file = File.expand_path("~/.git-set-pr.yml")

pr = YAML.load_file(settings_file)[Dir.pwd]

if pr
  puts pr
else
  exit -1
end
