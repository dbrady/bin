!#!/bin/sh
# mksparseimage - Because I can never remember the syntax to do it. This creates a 100m sparseimage.

# IMGNAME will have .sparseimage appended to it and will be the filename containing the volume.
IMGNAME=$1

# VOLNAME will be the name of the mounted volume.
VOLNAME=$2

hdiutil create -size 100m -volname $VOLUME -encryption -type SPARSE -fs HFS+ $IMGNAME
